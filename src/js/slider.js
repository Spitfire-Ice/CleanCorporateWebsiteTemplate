const sliderWrapperWidth = document.getElementById('sliderWrapper').offsetWidth;
const slider = document.getElementById('slider');
const sliderWidth = sliderWrapperWidth*3;

slider.style.width = ' ' + sliderWidth + 'px';




// console.log(sliderWidth);